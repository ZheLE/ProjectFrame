<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.frame.ui">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/DrawerTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".BaseActivity" >
        </activity>
        <activity android:name=".PhotoWallActivity" />
        <activity
            android:name=".DrawerMainActivity"
            android:label="@string/app_name"
            android:theme="@style/DrawerTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DrawerDetailActivity"
            android:label="@string/title_activity_drawer_detail"
            android:theme="@style/DrawerTheme">
        </activity>

        <!--Tab 2 详情页-->
        <activity
            android:name=".LruCacheDetailActivity"
            android:label="@string/title_activity_drawer_detail"
            android:theme="@style/ImpTheme">
        </activity>

    </application>

</manifest>
